**建議退回修訂。A 的交易框架可沿用，但 a、b 都不能原樣採用；B1 的「原子搬移、直接搬回」也不足以保證可回復。** 以下僅依程式與提案分析；資料庫現況採用題目提供的事實，未另行查證。

1. **High（高）｜A §4：b 會遺失真正的重放防線；a 的欄位合併口徑也不正確。**

   [capture-worker.ts:1484](/home/haha/CC_project/CC-memory/src/services/capture-worker.ts:1484) 會先以 `transcript_sources` 判斷完整涵蓋，命中即跳過寫入。`spool_offsets` 相同只避免部分追加；[實際程式](/home/haha/CC_project/CC-memory/src/services/capture-worker.ts:1505) 的 `summarize_count` 仍加一，`discovery_tokens` 則覆寫為本次抽取量，並非累計值。

   因此，「checkpoint（處理進度）已推進」只支持目前正常流程，不能涵蓋舊快照還原、狀態遺失或人工回放。選 b 後，已合併的舊區段可能再次寫入。

   **建議採修正版 a：**
   
   - 將已確認屬於目標的 `transcript_sources`，依 `path_hash` 分組，再按程式既有規則合併相鄰／重疊區間。
   - `observation_ids` 可去重聯集；不必因此變更讀取端。
   - 不直接跨不同緩衝檔聯集 `spool_offsets`：它只有位移，沒有檔案或世代識別。
   - 保留目標的 `discovery_tokens`；不要直接相加。`summarize_count` 是否相加須先定義歷史統計口徑。
   - 原始中繼資料完整保存在合併紀錄，供回復使用。

   `summary`／`embedding`（摘要向量）保持不動可接受，但須明示摘要仍只代表原有區段。

2. **High（高）｜A §4、重複檢查：目前「內容雜湊不撞」不能證明回放不會重複。**

   [capture-worker.ts:1363](/home/haha/CC_project/CC-memory/src/services/capture-worker.ts:1363) 計算 observation（觀察紀錄）的 `content_hash` 時包含 `projectId`。只搬 `project_id` 會保留舊專案算出的雜湊；同內容在新專案重新抽取，可能得到不同雜湊，繞過唯一鍵。

   **建議：**將題目的零碰撞結論限定為「本次搬移不撞既有唯一鍵」，不要擴張成語意去重保證。本次不必順便重算全部雜湊，但必須保留第 1 項的來源區間防線。

3. **High（高）｜A §1、§6：類 (4) 剩餘工作階段也缺少足夠的歸屬證據。**

   「同工作階段在某專案已有 active rollup（有效彙總記憶）」只證明目標存在，不證明崩塌列屬於它。題目已提供同工作階段跨兩個候選專案的反例；候選只有一個，也可能只是其他候選尚未被找到。

   [stepb-build-remap.ts](/home/haha/.cache/cc-memory/stepb-2026-09-03/stepb-build-remap.ts) 原本要求來源證據，沒有將既有彙總當成歸屬證明。

   **建議：**類 (4) 全部暫留人工核准，或補足逐來源的歸屬證據後再納入。不能只排除 `551de309`。

4. **High（高）｜A 回滾段：交易成功後的反向操作，不能無條件宣稱一致。**

   採修正版 a 後，worker（背景處理程序）會繼承合併後的來源覆蓋與觀察識別碼；反向搬走觀察列，目標仍可能保留它们的重放覆蓋。若直接還原整份舊中繼資料，又會抹掉合併後的新窗口。

   即使採 b，封存後的舊唯一鍵重新空出，也必須防止後續寫入重建相同鍵；否則恢復舊列為有效狀態會撞鍵。類 (2) 還須檢查對照表以外、後來指向存活者的觀察列，不能只檢查 key（識別鍵）。

   **建議：**明訂兩種回復：
   
   - 寫入者尚未恢復：同鎖、同交易重驗完整後狀態，再精確還原。
   - 已有後續寫入：拒絕直接倒跑對照表，另做人工拆分方案。

   保存實際舊 `merged_into`，不要假定可一律清空。此原則也符合 [前輪審查](/home/haha/.cache/cc-memory/stepb-2026-09-03/codex-r2c-out.md) 的限制。

5. **Medium（中）｜A §5：不能原封不動沿用 Step B 的殘餘集合檢查。**

   [stepb-apply.py:174](/home/haha/.cache/cc-memory/stepb-2026-09-03/stepb-apply.py:174) 計算崩塌列時沒有篩 `status`。本提案故意保留封存彙總的旧 `project_id`，所以它們仍在殘餘集合內。

   **建議：**對照表須明確列出「已處置但保留舊專案的封存列」與「尚未裁決列」，交易後驗證精確集合及每列後狀態。目標檢查也須包含正確專案、工作階段與完整 `idempotency_key`，不能只驗存在且有效。

   同一把 `flock`（程序互斥鎖）加兩表鎖，可保護遵守該鎖的背景程序；仍須確認沒有繞過鎖的執行入口。

6. **High（高）｜B1：鎖住背景程序，不等於鎖住掛鉤追加；兩個搬移也不是一筆原子操作。**

   [capture-spool.ts:114](/home/haha/CC_project/CC-memory/src/services/capture-spool.ts:114) 直接開檔追加，沒有取得背景程序的全域鎖。搬移後，已開啟的檔案仍可能收到追加；新的追加也可能重建原路徑。

   `.jsonl` 與狀態檔分別搬移，中途失敗會留下拆開的配對。之後「搬回即可」可能覆蓋新緩衝檔，或讓舊處理進度套到新檔案。

   **建議：**先確認所選工作階段沒有仍可追加的來源；在鎖內重驗清單，保存逐檔搬移紀錄，目的地存在即拒絕覆蓋。回復時也須重新鎖定、確認原路徑沒有新檔；有新資料就人工合併。隔離目錄須在來源確定靜止後才封存。

7. **Medium（中）｜B1：`copy-live` 可處理隔離目錄，但不是完整原始位元組備份。**

   [archive-capture-backlog.ts:824](/home/haha/CC_project/CC-memory/scripts/archive-capture-backlog.ts:824) 只保留 `.jsonl` 的有效完整行前綴；不完整尾端或錯誤行後資料可能被裁掉。驗證成功只證明封存符合這份裁切後清單。來源已刪也會記為 `missing`，不會因此讓整份封存失敗。

   另外，提案命令缺少 `--execute`，實際只會做預覽；見 [入口判斷](/home/haha/CC_project/CC-memory/scripts/archive-capture-backlog.ts:1435)。若在外層仍持有同一把鎖時直接啟動封存工具，其自行取鎖也可能失敗。

   **建議：**搬移完成並確認隔離內容靜止後，釋放外層鎖，再讓工具自行取鎖。執行方案補上 `--execute`；核對原始／保留位元組及來源缺失清單，保留隔離原件直到遠端驗證完成。暫存與新封存根目錄須同檔案系統，因為 [工具使用 `renameSync`](/home/haha/CC_project/CC-memory/scripts/archive-capture-backlog.ts:1321)。

8. **Medium（中）｜A §1、B §盤點與 B2：存活者選擇及處理量估計，都缺少決策口徑。**

   **類 (2)：**沿用既有彙總通常比「全封存再新增」少變動，也保留既有識別碼；但 `transcript_sources.start` 是檔案位移，不是建立時間。不同 `path_hash` 的起點不可直接比較。既然摘要不重算，選最早者還會留下較早區段的摘要。

   **建議：**先依摘要代表性及可查證時間選擇，再用固定識別碼作同分裁決。另建彙總只有在需要新摘要或明確分離合併產物時才有優勢，不會自動解決回滾問題。

   **B 的盲點：**題目列出的 `9,961 + 2,666 = 12,627`，與總數 `12,626` 不符。工作階段還可能包含多個來源路徑，須先確認分類互斥，並以「尚未處理區段」判斷來源是否全數缺失。`5 × 30 分鐘` 也不是持續占用運算時間；[重試程式](/home/haha/CC_project/CC-memory/src/services/capture-worker.ts:2111) 是分次延後嘗試。

   **建議：**先修正逐來源盤點，再提供專案預算表。「新的先做」支持不全面回放，尚不足以直接推導「其餘接受不可回復遺失」。B2 應讓使用者明確裁決保留或放棄；選定專案的 `--spool-dir` 仍須保留根目錄下的專案層級，不能把目錄名當成回放專案識別的覆寫參數。

本輪總結：已唯讀核對指定工具、背景寫入、重放與封存流程；未改檔、未連資料庫、未執行封存或測試。主要未決事項是來源歸屬、合併欄位口徑及寫入恢復後的回復契約；修訂後再審。

verdict: REQUEST-CHANGES